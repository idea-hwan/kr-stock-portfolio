#!/usr/bin/env bash
# TTM 수정 누적 성장률 — 단일 종목. 상세: docs/ttm_metric_growth.md
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT"
PY="${ROOT}/.venv/bin/python"
if [[ ! -x "$PY" ]]; then
  PY="python3"
fi
exec "$PY" scripts/screening/ttm_metric_growth.py "$@"
